#!/usr/bin/env node

process.env.NODE_ENV = process.env.NODE_ENV || 'test';

// bolt-test config file -> protractor
// read config file
// set config file to protractor config file
// run protractor
require('../../protractor/built/cli.js');
